export const APP_CONFIG = {
  JORNADA_MINUTES: {
    NORMAL: 424,
    REDUZIDO: 389,
  },
  MAX_PRODUCTION_ITEMS: 10,
  // Minimum similarity threshold to suggest an item in confirmation modal
  SIMILARITY_THRESHOLD: 0.80,
  // Threshold for classifying as High Similarity vs Moderate Similarity
  HIGH_SIMILARITY_THRESHOLD: 0.90,
};
